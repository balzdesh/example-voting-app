apiVersion: v1
kind: Pod
metadata:
  name: voting-pod
  labels:
    app: voting-app
    tier: frontend
spec:
  containers:
    - name: voting-app
      image: dockersamples/examplevotingapp_vote
      ports:
        - containerPort: 80
          name: vote
